/**
 * 프로젝트에 사용할 REGEX 모음
 */
export const PROJECT_REGEX = {
    // https://www.w3resource.com/javascript/form/email-validation.php
    EMAIL : /^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{2,3})+$/
};

/**
 * 프로젝트 API 통신 URL 주소
 */
export const PROJECT_API_URL = "http://localhost:8080";